import { getSyntaxDiffTemplate } from "@git-diff-view/react";

console.log("Type of getSyntaxDiffTemplate:", typeof getSyntaxDiffTemplate);
// We can't easily call it without real objects, but we can check if it's defined.
